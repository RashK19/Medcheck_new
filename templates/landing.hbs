<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MedCheck - Welcome</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
            background-color: #f4f6f9;
        }

        .navbar {
            background-color: #0077b6;
            padding: 10px;
        }
        .navbar-brand img {
            height: 40px;
        }

        .hero {
            height: 90vh;
            background: linear-gradient(rgba(0, 119, 182, 0.7), rgba(255, 255, 255, 0.7)), url('https://source.unsplash.com/1600x900/?hospital,healthcare');
            background-size: cover;
            background-position: center;
            color: #ffffff;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 0 20px;
        }
        .hero h1 {
            font-size: 4rem;
            font-weight: bold;
            margin-bottom: 20px;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
        }
        .hero p {
            font-size: 1.5rem;
            margin: 20px 0;
            max-width: 700px;
            line-height: 1.6;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
        }
        .hero .btn {
            font-size: 1.2rem;
            padding: 12px 35px;
            border-radius: 30px;
            background-color: #00b4d8;
            color: #ffffff;
            transition: background-color 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        }
        .hero .btn:hover {
            background-color: #0077b6;
            color: #ffffff;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

        .container1,footer {
            background-color: #0077b6;
            color: #ffffff;
            padding: 20px 0;
            text-align: center;
            font-size: 1rem;
        }
        footer p {
            margin: 0;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.8rem;
            }
            .hero p {
                font-size: 1.2rem;
            }
            .hero .btn {
                font-size: 1.1rem;
            }
        }
        img{
            width:200px;
            height:230px;
            border-radius: 50%;
            border: black solid 1px;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    

    <!-- Hero Section -->
    <section class="hero">
            <img src="/images/logo.png" alt="logo" style="max-width: 120px; margin-bottom: 20px;">
        
        <h1>Welcome to MedCheck</h1>
        <p>Your trusted partner for interpreting medical reports and tracking health. Join us in revolutionizing healthcare.</p>
        <a href="login" class="btn">Get Started</a>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 MedCheck. All rights reserved. | Your Health, Our Priority</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFmpleRz+Cb4QnUCIp2UQctH1Kz1Z7NQFjQIAqVGGw5ywllLvoFxjgOhZX" crossorigin="anonymous"></script>
</body>
</html>
